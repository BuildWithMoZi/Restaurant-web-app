/**
 * Homepage section rhythm (after Hero):
 * About — default/muted (not bright)
 * Signature — dark (fixed)
 * Menu — bright
 * Chef — dark (fixed)
 * Gallery — bright
 * Testimonials — dark (fixed)
 * Reservation — dark (fixed)
 */

/** Shared layout — padding, scroll offset, top border */
export const sectionShell = "section-shell";

/** Warm cream lift for zebra “bright” bands */
export const sectionBright = "section-surface-bright";

/** Dark navy bands — do not change without design review */
export const sectionDarkBg =
  "bg-[color-mix(in_oklab,var(--brand-secondary),transparent_94%)]";

export const sectionDarkBgDeep =
  "bg-[color-mix(in_oklab,var(--brand-secondary),transparent_96%)]";
